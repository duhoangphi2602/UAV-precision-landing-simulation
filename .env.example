# Docker Environment Setup

# Display
DISPLAY=${DISPLAY}
XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority}

# NVIDIA GPU
# Set to 'all' if nvidia runtime is available, else leave empty or comment out
# NVIDIA_VISIBLE_DEVICES=all
# NVIDIA_DRIVER_CAPABILITIES=all
