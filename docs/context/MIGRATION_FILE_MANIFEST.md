# MIGRATION_FILE_MANIFEST

> **Purpose:** Migration audit for the artifacts attached to this conversation. This document does not define a new roadmap and does not change the project. It records what each imported artifact was for, what decision it informed, whether later content superseded it, and whether a new agent should inspect the original.
>
> **Scope:** 31 user-imported conversation files listed by migration sequence, plus the surrounding conversation history. One generated file, `PROJECT_CONTINUITY.md`, is intentionally not included in the 31 imported-file count because it was generated by the assistant rather than imported by the user.
>
> **Evidence rule:** When the transcript or the imported file content does not prove an exact relationship, this manifest says `UNKNOWN` rather than inventing one. Status is based on the latest known project state, not merely filename order.

---

# A. MIGRATION SUMMARY

## Imported file count

- User-imported files: **31**
- Assistant-generated continuity file: **`PROJECT_CONTINUITY.md`** (separate from the 31)
- Imported artifacts include Markdown reports/plans, YAML configuration, CSV training metrics, and screenshots/images.

## High-level migration eras

1. **Phase 1 / camera + baseline recovery:** sequences 01–14.
2. **Slice 1 engineering foundation:** sequence 15 and related evidence; this period also includes the C++ audit/shadow/typed-interface transition described in surrounding conversation.
3. **Slice 2 moving ArUco platform:** sequences 16–19.
4. **Slice 3 baseline YOLO + ONNX:** sequence 20–25.
5. **Slice 3B small-object / single-class optimization:** sequences 26–27.
6. **Slice 3C A/B Pareto analysis:** sequence 28.
7. **Slice 4 TensorRT planning/toolchain remediation:** sequences 29–31.

## Important ordering warning

The suffixes in `implementation_plan(1).md`, `implementation_plan(3).md`, and `implementation_plan(4).md` are **attachment-name disambiguators**, not authoritative chronological labels by themselves. Chronology here is reconstructed primarily from upload time and surrounding conversation turn, then checked against content. For example:

- `implementation_plan.md` is the **Slice 3 baseline YOLO/ONNX** plan.
- `implementation_plan(1).md` is the **Slice 3B** plan.
- `implementation_plan(3).md` is the **Slice 4 TensorRT FP32/FP16/INT8** plan before toolchain remediation.
- `implementation_plan(4).md` is the later **Slice 4 toolchain remediation** plan.

The suffix is therefore not a semantic version.

---

# B. FILE-BY-FILE MANIFEST

## 01 — `camera_gate_report.md`

**Sequence:** 01  
**Filename:** `camera_gate_report.md`  
**Approximate project phase:** Early camera bring-up / Phase 1.5 recovery  
**Why introduced:** Record the first camera gate result after restoring camera streaming.  
**Question/problem:** Is the Gazebo camera topic alive and can ROS 2 receive frames, and can OpenCV detect the ArUco marker?  
**Important result:** Camera stream worked, `/camera` received frames, `debug_frame.png` was saved, but ArUco detection failed. The report identifies the camera topic, `gz.msgs.Image`, the `ros_gz_image` bridge, and a low CPU-rendered frame rate. It also identifies PBR/software-rendering and ground-plane/Z-fighting as possible causes. fileciteturn22file0L9-L30 fileciteturn22file0L32-L50  
**Effect on decisions:** Led to the next camera-orientation/rendering investigation rather than treating ROS image transport as the blocker.  
**Current relevance:** Historical evidence of the early camera blocker.  
**Status:** **HISTORICAL / SUPERSEDED**  
**Related architecture/component:** Gazebo camera, `ros_gz_image`, OpenCV/ArUco.  
**Related decision:** Camera gate must separate transport success from visual-content success.  
**Related files:** `camera_gate_report(1).md`, `debug_frame.png`, `snapshot_report.md`.  
**Equivalent/current content in Git repo?:** **UNKNOWN**; likely later source/config supersedes the report, but the historical report itself is not current truth.  
**Must new agent inspect original?:** **OPTIONAL**; inspect only if reconstructing early camera debugging.  
**Risk if omitted:** Low for current implementation; moderate for understanding why early camera patches were discarded.

---

## 02 — `image(58).png`

**Sequence:** 02  
**Filename:** `image(58).png`  
**Approximate project phase:** Early camera gate, before camera orientation correction  
**Why introduced:** Visual evidence corresponding to the early camera frame.  
**Question/problem:** What did the camera actually see when the camera gate was failing?  
**Important result:** The image shows a largely featureless/sky-like view rather than the landing ground/marker. This is consistent with the first camera gate report saying the stream existed but marker detection failed. fileciteturn25file0L1-L6  
**Effect on decisions:** Supported investigation of camera orientation rather than ROS transport.  
**Current relevance:** Historical visual evidence.  
**Status:** **HISTORICAL / EVIDENCE**  
**Related architecture/component:** Gazebo x500 camera.  
**Related decision:** Camera optical-axis correction had to be investigated.  
**Related files:** `camera_gate_report.md`, `camera_gate_report(1).md`.  
**Equivalent/current content in Git repo?:** **UNKNOWN**.  
**Must new agent inspect original?:** **NO**, unless reproducing the original camera failure.  
**Risk if omitted:** Low.

---

## 03 — `camera_gate_report(1).md`

**Sequence:** 03  
**Filename:** `camera_gate_report(1).md`  
**Approximate project phase:** Early camera gate, after optical-axis correction  
**Why introduced:** Updated camera-gate evidence after the camera orientation issue was identified.  
**Question/problem:** Does the camera now look at the ground after changing the problematic orientation?  
**Important result:** The updated report states that the camera pitch was changed from `1.5707` to `-1.5707` and the camera then looked downward; the ground became visible, while marker detection remained a separate next step. fileciteturn22file1L61-L80  
**Effect on decisions:** Established that the camera optical-axis mapping itself was the early blocker. The later project intentionally stopped relying on runtime `sed` camera patches because exact ±90° manipulation caused instability.  
**Current relevance:** Historical evidence only.  
**Status:** **HISTORICAL / SUPERSEDED**  
**Related architecture/component:** Gazebo x500 camera SDF, camera orientation.  
**Related decision:** Do not conflate camera pointing with marker detection.  
**Related files:** `camera_gate_report.md`, `debug_frame.png`.  
**Equivalent/current content in Git repo?:** **UNKNOWN**.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Low, but new agent may miss the original camera-coordinate discovery.

### Relationship: `camera_gate_report.md` vs `camera_gate_report(1).md`

They are **two snapshots of the same camera-gate investigation**, not two unrelated reports and not a simple duplicate pair. The first records **camera transport PASS / marker detection FAIL**; the suffixed report records a later state where **camera ground view PASS**, after the orientation issue was corrected. The suffix is therefore not “newest forever”; it reflects a later attachment with revised content. The first is an earlier failure checkpoint, the second is a later recovery checkpoint. The later report superseded the earlier camera status but not its historical value. fileciteturn22file0L11-L13 fileciteturn22file1L61-L79

---

## 04 — `debug_frame.png`

**Sequence:** 04  
**Filename:** `debug_frame.png`  
**Approximate project phase:** Early camera/debug evidence  
**Why introduced:** Saved camera frame used to inspect what the vehicle camera was rendering.  
**Question/problem:** Is the image stream real and what is visually present in the camera buffer?  
**Important result:** The uploaded image shows a rendering of the drone/scene rather than a valid landing-marker view for the early failed gate. The report explicitly records the frame as a successful frame capture despite marker-detection failure. fileciteturn22file0L32-L40  
**Effect on decisions:** Helped separate image transport from actual visual perception quality.  
**Current relevance:** Debug evidence; the root-level/debug file was later removed from Git hygiene cleanup.  
**Status:** **HISTORICAL / SUPERSEDED**  
**Related architecture/component:** Camera → ROS → OpenCV.  
**Related decision:** Do not keep root debug image artifacts in the public repository.  
**Related files:** `camera_gate_report.md`, `image(58).png`, `snapshot_report.md`.  
**Equivalent/current content in Git repo?:** **NO as a tracked final artifact**; the earlier repo copy was explicitly removed during final hygiene according to the conversation.  
**Must new agent inspect original?:** **NO** for current development.  
**Risk if omitted:** Low.

---

## 05 — `snapshot_report.md`

**Sequence:** 05  
**Filename:** `snapshot_report.md`  
**Approximate project phase:** Early project state snapshot / debugging handoff  
**Why introduced:** Provide a snapshot of repository/runtime state while the agent had quotas/work interruptions.  
**Question/problem:** What was the project state at that moment, and what files were changing?  
**Important result:** It was used as a source-of-truth snapshot during early debugging. A later screenshot shows the project in the middle of a Python baseline demo with Gazebo open and a quota/task interruption. fileciteturn25file2L13-L18  
**Effect on decisions:** Helped recover context during long-running agent tasks.  
**Current relevance:** Historical snapshot only; it was explicitly treated as a generated debug artifact later and removed from Git tracking during final cleanup.  
**Status:** **HISTORICAL / SUPERSEDED**  
**Related architecture/component:** Repository state, early baseline demo.  
**Related decision:** Snapshot reports are useful for recovery but not canonical project documentation.  
**Related files:** `image(59).png`, `image(60).png`, `Pasted text(3).txt`.  
**Equivalent/current content in Git repo?:** **NO as a tracked final artifact**; later continuity/evidence docs superseded it.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Low to moderate for reconstructing the quota/recovery history.

---

## 06 — `image(59).png`

**Sequence:** 06  
**Filename:** `image(59).png`  
**Approximate project phase:** Early baseline recovery/debugging  
**Why introduced:** Screenshot of the AI-agent IDE state during baseline debugging.  
**Question/problem:** What was the agent doing while the project was being brought back to a working baseline?  
**Important result:** Shows the AI tool waiting through mission phases and eventually hitting an individual quota error, while `inspection.sdf` and `run_demo_python_baseline.sh` were being changed. fileciteturn25file2L13-L18  
**Effect on decisions:** This explains why recovery/snapshot artifacts were needed and why baseline runs had to be carefully resumed.  
**Current relevance:** Historical workflow evidence.  
**Status:** **HISTORICAL / EVIDENCE**  
**Related architecture/component:** Baseline mission runner / Gazebo world.  
**Related decision:** Avoid unnecessary agent-run loops; prefer short explicit gates.  
**Related files:** `snapshot_report.md`, `image(60).png`, `Pasted text(3).txt`.  
**Equivalent/current content in Git repo?:** **NO**.  
**Must new agent inspect original?:** **NO**.  
**Risk if omitted:** Very low.

---

## 07 — `image(60).png`

**Sequence:** 07  
**Filename:** `image(60).png`  
**Approximate project phase:** Python baseline recovery / first full visual demo attempts  
**Why introduced:** Screenshot showing the project tree, Gazebo window, IDE, and agent activity during baseline stabilization.  
**Question/problem:** What was the concrete runtime state and what files were being touched during early baseline debugging?  
**Important result:** Shows Gazebo open, `run_demo_python_baseline.sh`, the `aruco_marker_1` entity, and the mission around the camera/waypoint debugging stage. It is a strong historical visual anchor for the early architecture.  
**Effect on decisions:** Reinforced the separation between simulation, scripts, and the ROS/PX4 pipeline.  
**Current relevance:** Historical visual evidence only.  
**Status:** **HISTORICAL / EVIDENCE**  
**Related architecture/component:** Gazebo, PX4 SITL, Python baseline script.  
**Related decision:** Keep baseline runner stable and debug the runtime rather than redesigning the project.  
**Related files:** `image(59).png`, `image(61).png`, `image(62).png`, `image(63).png`.  
**Equivalent/current content in Git repo?:** **NO**.  
**Must new agent inspect original?:** **NO**.  
**Risk if omitted:** Low.

---

## 08 — `image(61).png`

**Sequence:** 08  
**Filename:** `image(61).png`  
**Approximate project phase:** First visually successful ArUco camera frame  
**Why introduced:** Screenshot/frame showing the drone and ArUco marker after camera debugging.  
**Question/problem:** Can the camera actually see the landing marker?  
**Important result:** The image shows a drone at left and a visible ArUco marker centered in the frame, proving the visual perception path could finally see the marker. fileciteturn25file4L26-L30  
**Effect on decisions:** This is a key precursor to the later successful ArUco detection / precision-landing baseline.  
**Current relevance:** Historical proof of first successful marker visibility.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** Camera, Gazebo, ArUco/OpenCV.  
**Related decision:** Proceed from camera gate to actual marker detection/control integration.  
**Related files:** `camera_gate_report(1).md`, `image(62).png`, `image(63).png`.  
**Equivalent/current content in Git repo?:** **UNKNOWN**.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Moderate for understanding the first visual success, low for current code.

---

## 09 — `image(62).png`

**Sequence:** 09  
**Filename:** `image(62).png`  
**Approximate project phase:** ArUco detection debug, first overlay evidence  
**Why introduced:** Screenshot showing the detector's visual overlay after marker detection.  
**Question/problem:** Is the marker actually being detected and is the error computation visible?  
**Important result:** The frame shows the green detection box and center/error overlay around the marker, with `dx: 0.5 dy: 0.5` visible. fileciteturn26file0L1-L6  
**Effect on decisions:** Provided evidence that ArUco detection and pixel-error calculation were working before the control pipeline was finalized.  
**Current relevance:** Historical perception evidence.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** `aruco_detector.py`, debug visualization.  
**Related decision:** Proceed to connect the detector output into control logic.  
**Related files:** `image(61).png`, `image(63).png`, later Slice 1 dashboard.  
**Equivalent/current content in Git repo?:** **UNKNOWN**.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Low to moderate.

---

## 10 — `image(63).png`

**Sequence:** 10  
**Filename:** `image(63).png`  
**Approximate project phase:** Gazebo + OpenCV viewer visual integration  
**Why introduced:** Screenshot showing the Gazebo world and the separate `Drone Camera View` window.  
**Question/problem:** Can the user simultaneously see the 3D Gazebo world and the camera/OpenCV output?  
**Important result:** The screenshot shows the Gazebo Sim window, the drone, the landing marker in the world, and a separate `Drone Camera View` OpenCV window. This became an important visual objective of the project. fileciteturn26file1L8-L12  
**Effect on decisions:** Supported the later requirement that the portfolio demo visibly combine Gazebo and OpenCV.  
**Current relevance:** Historical proof of the two-window visual demo.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** Gazebo viewer, OpenCV viewer.  
**Related decision:** Maintain a visible dual-window demo rather than hiding all data in logs.  
**Related files:** `image(62).png`, `Screenshot From 2026-07-27 21-03-33.png`, `walkthrough.md`.  
**Equivalent/current content in Git repo?:** **UNKNOWN**.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Moderate for portfolio/demo continuity, low for runtime correctness.

---

## 11 — `REPORT_C1.md`

**Sequence:** 11  
**Filename:** `REPORT_C1.md`  
**Approximate project phase:** C++ control audit / pre-Slice 1 engineering transition  
**Why introduced:** Audit `precision_landing_control_cpp` before replacing Python control with C++.  
**Question/problem:** Does the C++ PID implementation have the safety/math primitives needed for production-style control?  
**Important result:** PID reset, deadband, saturation, integral clamp, derivative first-sample handling, stale observation timeout, target-loss reset, and NaN/Inf protection all existed; 10 functional GTests passed. Lint/style tests were failing but documented. fileciteturn23file0L9-L24  
**Effect on decisions:** Enabled Shadow Mode and eventual C++ control transition without refactoring the PID core.  
**Current relevance:** Historical engineering evidence; the C++ package remains current but the report itself is superseded by later acceptance reports.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** `precision_landing_control_cpp`, GTest, PID.  
**Related decision:** Keep C++ PID, document lint limitation instead of blocking functional progress.  
**Related files:** `REPORT_C2(1).md`, Slice 1 `walkthrough.md`, later Git tag `slice1-engineering-interface-v1`.  
**Equivalent/current content in Git repo?:** **YES in source/tests; report likely local evidence only.**  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Low; the test result is useful but not current code truth.

---

## 12 — `Pasted text(3).txt`

**Sequence:** 12  
**Filename:** `Pasted text(3).txt`  
**Approximate project phase:** Long-running baseline/C++ demo log capture  
**Why introduced:** Preserve terminal/log excerpts from a real `make demo-cpp` run and repository artifact timestamps.  
**Question/problem:** What happened in the runtime, what containers/logs existed, and where did the mission state reach?  
**Important result:** It records artifact ordering and grep evidence for `ARM → TAKEOFF → NAVIGATE → SCAN → ALIGN → DESCEND → LAND` plus container/build state. fileciteturn26file3L27-L47 fileciteturn26file3L77-L146  
**Effect on decisions:** Supported diagnosing mission completion/termination and later cleanup.  
**Current relevance:** Historical runtime evidence only.  
**Status:** **HISTORICAL / EVIDENCE**  
**Related architecture/component:** Demo scripts, Docker containers, mission state machine.  
**Related decision:** Mission scripts must wait for true terminal/disarm completion before cleanup.  
**Related files:** `snapshot_report.md`, `REPORT_C2.md` raw artifacts, `REPORT_C3.md` raw artifacts (mentioned inside the pasted text).  
**Equivalent/current content in Git repo?:** **UNKNOWN**; the raw logs were local ignored artifacts.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Low to moderate for understanding historical termination debugging.

---

## 13 — `ENGINEERING_UPGRADE_READINESS_REPORT.md`

**Sequence:** 13  
**Filename:** `ENGINEERING_UPGRADE_READINESS_REPORT.md`  
**Approximate project phase:** Pre-engineering-upgrade audit before Slice 1  
**Why introduced:** Snapshot the existing project and determine readiness for typed ROS, dashboard, YOLO, TensorRT, and gimbal expansion.  
**Question/problem:** What is the current architecture, what is missing, and where are the real blockers?  
**Important result:** It freezes the then-current Python/C++ pipeline, explicitly says `demo-python` remains independent of C++, identifies ROS 2 topics as the communication mechanism, records responsibility boundaries, identifies the camera-fixed/no-gimbal state, missing ML/TensorRT stack, and proposes the original engineering upgrade slices. fileciteturn23file1L36-L70 fileciteturn23file1L86-L102 fileciteturn23file1L122-L153  
**Effect on decisions:** Strong architectural baseline for the later typed-interface/dashboard roadmap.  
**Current relevance:** Historical architecture snapshot; later slices changed the status of typed interfaces, dashboard, ML stack, and moving ArUco.  
**Status:** **HISTORICAL / SUPERSEDED**  
**Related architecture/component:** Entire architecture.  
**Related decision:** Python mission orchestration + C++ PID + ROS 2 boundary.  
**Related files:** `REPORT_C1.md`, `walkthrough.md`, `implementation_plan.md`.  
**Equivalent/current content in Git repo?:** **PARTIAL / YES** — later source and slice reports embody the decisions, but the original readiness snapshot itself is historical.  
**Must new agent inspect original?:** **YES** for historical architecture rationale; **not as current source of truth**.  
**Risk if omitted:** High: a new agent may misunderstand why the project is intentionally split between Python and C++ and may rediscover already-known blockers.

---

## 14 — `Screenshot From 2026-07-27 21-03-33.png`

**Sequence:** 14  
**Filename:** `Screenshot From 2026-07-27 21-03-33.png`  
**Approximate project phase:** Slice 1 dashboard correction / visual acceptance  
**Why introduced:** Visual evidence of the OpenCV dashboard during C++ control.  
**Question/problem:** Is the dashboard visible, and what does the actual GUI look like?  
**Important result:** It shows the `Drone Camera View` window with a side telemetry panel, mode buttons, target/error information, controller text, and the ArUco target. The image also visually shows the readability problem that later motivated dashboard layout/readability corrections. fileciteturn26file2L14-L18  
**Effect on decisions:** Contributed to tightening the dashboard UI and later separating camera FPS from UI refresh rate.  
**Current relevance:** Historical visual evidence.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** `camera_viewer.py`, OpenCV dashboard.  
**Related decision:** Keep the dashboard user-visible and operational, then improve readability/performance without changing core control architecture.  
**Related files:** `walkthrough.md`, Slice 1 source, later dashboard metrics.  
**Equivalent/current content in Git repo?:** **UNKNOWN**.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Moderate for UI history, low for core runtime.

---

## 15 — `walkthrough.md`

**Sequence:** 15  
**Filename:** `walkthrough.md`  
**Approximate project phase:** Slice 1 engineering interface/dashboard completion, with intermediate low-FPS calibration evidence  
**Why introduced:** Human-readable walkthrough of the engineering upgrade and runtime behavior.  
**Question/problem:** What exactly changed in Slice 1 and how did the Python/C++/dashboard/metrics system fit together?  
**Important result:** It documents typed ROS messages, backwards compatibility, dashboard, metrics, C++ typed path, and the low-FPS calibration experiments. fileciteturn22file2L96-L122  
**Effect on decisions:** Served as a transition/evidence document while Slice 1 evolved toward final closure.  
**Current relevance:** **Historical**, because the current project has since merged Slice 1 and Slice 2 and has newer canonical acceptance docs.  
**Status:** **HISTORICAL / SUPERSEDED**  
**Related architecture/component:** Slice 1 typed interface, C++ PID, dashboard.  
**Related decision:** Keep Python/C++ boundaries stable; use metrics to distinguish camera FPS from UI Hz.  
**Related files:** `ENGINEERING_UPGRADE_READINESS_REPORT.md`, `Screenshot From 2026-07-27 21-03-33.png`, `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`.  
**Equivalent/current content in Git repo?:** **YES, functionally**; later merged source and evidence supersede the walkthrough's intermediate claims.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Moderate because this file contains some intermediate Slice 1 history that explains later corrections.

---

## 16 — `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`

**Sequence:** 16  
**Filename:** `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`  
**Approximate project phase:** Slice 2 moving ArUco platform landing  
**Why introduced:** Acceptance record for one-dimensional moving landing platform.  
**Question/problem:** Can the existing typed ROS 2 + C++ PID + dashboard + metrics foundation track and land on a target moving at 0.10 m/s without breaking fixed landing?  
**Important result:** The final uploaded version says Slice 2 PASS, platform movement at 0.10 m/s, safety gates, re-align count, moving touchdown error ~0.067 m, and fixed regression touchdown error ~0.025 m. fileciteturn22file3L154-L188  
**Effect on decisions:** Established moving ArUco landing as a completed milestone.  
**Current relevance:** Current historical acceptance baseline; Slice 2 is merged and should not be rediscovered.  
**Status:** **CURRENT MILESTONE EVIDENCE**  
**Related architecture/component:** `inspection_moving.sdf`, `MovingPlatformState.msg`, `moving_platform_controller.py`, mission state machine.  
**Related decision:** Moving platform speed target 0.10 m/s; no YOLO/TensorRT/Kalman in Slice 2.  
**Related files:** `pid_moving.yaml`, `REPORT_C2(1).md`, `walkthrough(1).md`.  
**Equivalent/current content in Git repo?:** **YES at source/architecture level; the report itself may be local-only evidence.**  
**Must new agent inspect original?:** **YES** for Slice 2 acceptance history if the new agent needs to modify moving-mode behavior.  
**Risk if omitted:** High: agent may retune or reimplement an already-PASS moving landing.

---

## 17 — `walkthrough(1).md`

**Sequence:** 17  
**Filename:** `walkthrough(1).md`  
**Approximate project phase:** Slice 2 moving ArUco completion / final walkthrough  
**Why introduced:** A later walkthrough corresponding to the moving-platform slice, not a continuation of the Slice 1 walkthrough with no changes.  
**Question/problem:** How does the completed moving ArUco slice work end-to-end, including moving-platform control, interface propagation, dashboard, safety gates, and acceptance?  
**Important result:** The conversation explicitly linked it to the moving-platform slice and the final completion using contact-confirmed touchdown.  
**Effect on decisions:** Provides human-readable continuity for Slice 2.  
**Current relevance:** Relevant as historical acceptance explanation; source code is the real implementation truth.  
**Status:** **EVIDENCE / CURRENT MILESTONE CONTEXT**  
**Related architecture/component:** Moving platform, typed messages, C++ PID, mission controller, dashboard.  
**Related decision:** Moving tracking is reactive visual tracking; platform ground truth is for telemetry/metrics, not flight control.  
**Related files:** `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`, `pid_moving.yaml`, `REPORT_C2(1).md`.  
**Equivalent/current content in Git repo?:** **YES functionally; report itself may be ignored/local.**  
**Must new agent inspect original?:** **YES if modifying Slice 2**; otherwise optional.  
**Risk if omitted:** Moderate to high for preserving moving-slice rationale.

### Relationship: `walkthrough.md` vs `walkthrough(1).md`

These are **not simple revisions of the same Slice**. The surrounding history identifies `walkthrough.md` with the earlier Slice 1 engineering/dashboard work and intermediate fixed-landing calibration; `walkthrough(1).md` is associated with the later moving ArUco platform Slice 2. Therefore the suffix does not mean “latest version of the same document”; it reflects a separate later attachment with overlapping filename semantics. The file content is needed to distinguish the exact revision because filenames alone are ambiguous. 

---

## 18 — `pid_moving.yaml`

**Sequence:** 18  
**Filename:** `pid_moving.yaml`  
**Approximate project phase:** Slice 2 moving tracking tuning/debugging  
**Why introduced:** Separate moving-platform PID profile so fixed baseline settings could remain protected.  
**Question/problem:** What control gains and signs should be used when tracking a platform moving at 0.10 m/s?  
**Important result:** The uploaded version shows a moving profile with `kp_x/y=0.0025`, `ki_x/y=0.0001`, `kd=0`, `deadband=0`, output ±1.0, integral ±1500, stale timeout 1.0, `flip_x=false`, `flip_y=false`. fileciteturn22file4L199-L215  
**Effect on decisions:** This exact uploaded config was later superseded: Run 6 proved the false flips were the wrong sign mapping; the final accepted moving profile restored `flip_x=true`, `flip_y=true`, with more conservative gains and output limits. fileciteturn23file2L200-L222  
**Current relevance:** **Historical configuration evidence only; do not copy it as current truth.**  
**Status:** **SUPERSEDED**  
**Related architecture/component:** C++ PID moving profile.  
**Related decision:** Body-frame command interpretation and safe axis mapping.  
**Related files:** `REPORT_C2(1).md`, `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`.  
**Equivalent/current content in Git repo?:** **YES in later code/config, but this exact uploaded version is superseded.**  
**Must new agent inspect original?:** **OPTIONAL**, only when studying why Run 6 was unstable.  
**Risk if omitted:** Moderate because a new agent could otherwise repeat the `flip_x=false/flip_y=false` mistake.

---

## 19 — `REPORT_C2(1).md`

**Sequence:** 19  
**Filename:** `REPORT_C2(1).md`  
**Approximate project phase:** Slice 2 control reset, bounded touchdown, sign mapping diagnosis  
**Why introduced:** Preserve detailed moving-platform control evidence after several unstable runs.  
**Question/problem:** Why was moving tracking unstable, what is the command frame, and what configuration is actually safe?  
**Important result:** Run 6 with `flip_x=false/flip_y=false` was marked unstable/superseded; the command frame was verified as BODY; the safe final mapping was restored to `flip_x=true`, `flip_y=true`, `swap_axes=false`; tracking-only verification passed at ~0.09 m/s; fixed pixel thresholds at low altitude caused failures. fileciteturn23file2L198-L244  
**Effect on decisions:** This is a major decision artifact explaining the final moving control sign convention and the decision not to use Kalman at that point.  
**Current relevance:** High historical importance for Slice 2 rationale; not the current runtime source of truth.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** `precision_landing_control_cpp`, moving platform, mission state machine.  
**Related decision:** BODY frame; `flip_x=true`, `flip_y=true`; conservative PI; no Kalman in the accepted Slice 2 path.  
**Related files:** `pid_moving.yaml`, `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`, `walkthrough(1).md`.  
**Equivalent/current content in Git repo?:** **PARTIAL / YES** at config/code level; report is historical.  
**Must new agent inspect original?:** **YES** when modifying moving tracking or sign conventions.  
**Risk if omitted:** High.

---

## 20 — `implementation_plan.md`

**Sequence:** 20  
**Filename:** `implementation_plan.md`  
**Approximate project phase:** Slice 3 YOLO baseline planning  
**Why introduced:** Plan the first ML slice after fixed/moving ArUco control was stable.  
**Question/problem:** How do we train one reproducible YOLO baseline, evaluate it, export ONNX, and establish a reference before TensorRT?  
**Important result:** It defines `ml/configs`, `ml/datasets`, `ml/scripts`, `ml/experiments`, `ml/exports`, `ml/reports`, dataset validation, visual sanity, YOLOv8n at 640/50 epochs, ONNX export, parity, and manifest. It initially blocked on Roboflow access until the local archive was supplied. fileciteturn23file3L256-L309  
**Effect on decisions:** Established Slice 3 as an independently reproducible ML baseline before optimizing small-object detection or TensorRT.  
**Current relevance:** Historical plan; Slice 3 baseline and later 3B/3C are already completed and merged.  
**Status:** **SUPERSEDED**  
**Related architecture/component:** ML workspace and YOLO/ONNX pipeline.  
**Related decision:** Train baseline first; do not jump directly into TensorRT.  
**Related files:** `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`, `results.csv`, `implementation_plan(1).md`.  
**Equivalent/current content in Git repo?:** **YES functionally** through scripts/config/manifest, but not as this exact plan.  
**Must new agent inspect original?:** **NO for execution; OPTIONAL for provenance.**  
**Risk if omitted:** Low for current work; moderate if rebuilding the ML evolution history.

---

## 21 — `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`

**Sequence:** 21  
**Filename:** `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`  
**Approximate project phase:** Slice 3 baseline YOLO training + ONNX export acceptance  
**Why introduced:** Close the baseline ML pipeline before small-object optimization.  
**Question/problem:** Is the baseline YOLOv8n training/data/ONNX pipeline reproducible and usable as a reference?  
**Important result:** 6469 train / 547 val / 1610 test, four classes; dataset validation PASS; YOLOv8n 50 epochs, imgsz 640, batch 16; mAP50 0.501, mAP50-95 0.335; ONNX opset 20 dynamic false batch 1; ONNX parity average IoU 0.96. fileciteturn23file4L319-L340  
**Effect on decisions:** Established the weak 4-class baseline that motivated data-centric analysis and single-class/960 optimization.  
**Current relevance:** Historical baseline evidence; do not use its 0.501 as the final deployment model metric.  
**Status:** **EVIDENCE / HISTORICAL BASELINE**  
**Related architecture/component:** YOLOv8n, PyTorch, ONNX.  
**Related decision:** Baseline is a frozen reference; later Candidate A is the deployment model.  
**Related files:** `confusion_matrix_normalized.png`, `labels.jpg`, `results.csv`, `val_batch0_labels.jpg`, `val_batch0_pred.jpg`, `implementation_plan(1).md`.  
**Equivalent/current content in Git repo?:** **YES as model-pipeline source/config, NO as the current model result.**  
**Must new agent inspect original?:** **YES for understanding baseline-to-Candidate-A improvement.**  
**Risk if omitted:** High for AI-engineer continuity; agent might compare against the wrong baseline or skip the data-centric reasoning.

---

## 22 — `confusion_matrix_normalized.png`

**Sequence:** 22  
**Filename:** `confusion_matrix_normalized.png`  
**Approximate project phase:** Slice 3 baseline model diagnosis  
**Why introduced:** Visual evidence of class confusion in the 4-class baseline.  
**Question/problem:** Is poor baseline accuracy mostly a classification confusion problem?  
**Important result:** It shows strong `car ↔ van` and `car ↔ truck` confusion and background-related errors; this supported the decision to collapse classes for a vehicle-tracking model.  
**Effect on decisions:** One of the key visual reasons for moving to a single `vehicle` class for the gimbal detector.  
**Current relevance:** Historical evidence; the final deployment model is single-class.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** YOLOv8n baseline classifier/detector.  
**Related decision:** Single-class vehicle for gimbal tracking.  
**Related files:** `results.csv`, `labels.jpg`, `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`, `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`.  
**Equivalent/current content in Git repo?:** **NO as public tracked artifact; concept reflected in analysis/report history.**  
**Must new agent inspect original?:** **OPTIONAL** but useful when revisiting class imbalance.  
**Risk if omitted:** Moderate.

---

## 23 — `labels.jpg`

**Sequence:** 23  
**Filename:** `labels.jpg`  
**Approximate project phase:** Slice 3 dataset/baseline diagnostic  
**Why introduced:** Visual summary of label distribution / object sizes for the baseline dataset.  
**Question/problem:** Is the dataset class-imbalanced and dominated by small objects?  
**Important result:** It supports the large `car` dominance and small-bbox concentration later quantified in Slice 3B.  
**Effect on decisions:** Helped move the project from naive retraining toward explicit data-centric analysis and input-resolution optimization.  
**Current relevance:** Historical evidence of dataset diagnosis.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** Dataset + YOLO training.  
**Related decision:** Single-class collapse and `imgsz=960` became Candidate A.  
**Related files:** `results.csv`, `confusion_matrix_normalized.png`, `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`.  
**Equivalent/current content in Git repo?:** **NO as tracked image; corresponding quantitative analysis is retained in Slice 3B documentation.**  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Low to moderate.

---

## 24 — `results.csv`

**Sequence:** 24  
**Filename:** `results.csv`  
**Approximate project phase:** Slice 3 YOLO baseline training history  
**Why introduced:** Preserve per-epoch training/validation metrics so the baseline could be independently analyzed.  
**Question/problem:** How did the baseline learn, when did it converge, and what were the final metrics?  
**Important result:** It was later used to compare Candidate A/B learning curves and to understand why Candidate A started with a much stronger metric after transfer learning.  
**Effect on decisions:** Supported the explanation that Candidate A's higher starting metric was due to transfer from the earlier UAVDT baseline, while Candidate B's lower start came from a different P2 initialization path.  
**Current relevance:** Historical training evidence.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** YOLO training.  
**Related decision:** Transfer learning is part of the intended AI-engineering workflow; do not infer model quality from curve shape alone.  
**Related files:** `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`, `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`, `SLICE_3C_PARETO_OPTIMIZATION.md`.  
**Equivalent/current content in Git repo?:** **NO as a tracked generated artifact.**  
**Must new agent inspect original?:** **OPTIONAL**; **YES** if reproducing learning-curve causality.  
**Risk if omitted:** Moderate for understanding training evolution.

---

## 25 — `val_batch0_labels.jpg`

**Sequence:** 25  
**Filename:** `val_batch0_labels.jpg`  
**Approximate project phase:** Slice 3 baseline visual validation  
**Why introduced:** Visual check of ground-truth labels over validation images.  
**Question/problem:** Are labels aligned with images before accepting model training?  
**Important result:** Visual evidence that the exported YOLO annotations overlay correctly on the drone imagery.  
**Effect on decisions:** Supported passing dataset sanity before optimization.  
**Current relevance:** Historical evidence.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** Dataset labeling / YOLO.  
**Related decision:** Visual sanity is a gate before training.  
**Related files:** `val_batch0_pred.jpg`, `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`.  
**Equivalent/current content in Git repo?:** **NO** as tracked generated output.  
**Must new agent inspect original?:** **NO** unless auditing annotation history.  
**Risk if omitted:** Low.

---

## 26 — `val_batch0_pred.jpg`

**Sequence:** 26  
**Filename:** `val_batch0_pred.jpg`  
**Approximate project phase:** Slice 3 baseline visual validation  
**Why introduced:** Visual check of model predictions against validation images.  
**Question/problem:** Do predictions visually make sense before declaring the baseline usable?  
**Important result:** Demonstrates the model's visual behavior and was part of the acceptance evidence set.  
**Effect on decisions:** Supported visual sanity and later error analysis.  
**Current relevance:** Historical baseline evidence.  
**Status:** **EVIDENCE / HISTORICAL**  
**Related architecture/component:** YOLOv8n inference.  
**Related decision:** Evaluate visually, not only by one aggregate scalar metric.  
**Related files:** `val_batch0_labels.jpg`, `results.csv`, `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`.  
**Equivalent/current content in Git repo?:** **NO** as tracked artifact.  
**Must new agent inspect original?:** **OPTIONAL**.  
**Risk if omitted:** Low to moderate.

---

## 27 — `implementation_plan(1).md`

**Sequence:** 27  
**Filename:** `implementation_plan(1).md`  
**Approximate project phase:** Slice 3B small-object + single-class optimization plan  
**Why introduced:** Plan the data-centric correction after the weak 4-class baseline.  
**Question/problem:** Should the project improve the detector by class consolidation, higher input size, P2, and/or SAHI before TensorRT?  
**Important result:** It defines single-class `vehicle`, `imgsz=960`, Candidate A, conditional P2, and SAHI benchmark, and emphasizes manual foreground training. fileciteturn24file0L9-L16 fileciteturn24file0L22-L45  
**Effect on decisions:** Led directly to Candidate A (`YOLOv8n`, 960, single class), then conditional Candidate B and SAHI benchmarking.  
**Current relevance:** Historical plan; Slice 3B/3C is already complete.  
**Status:** **SUPERSEDED**  
**Related architecture/component:** YOLOv8n/P2/SAHI, dataset transformation.  
**Related decision:** Do not apply every optimization simultaneously; use evidence-driven gates.  
**Related files:** `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`, `SLICE_3C_PARETO_OPTIMIZATION.md`, `implementation_plan.md`.  
**Equivalent/current content in Git repo?:** **YES functionally through scripts/configs, but the exact plan is superseded.**  
**Must new agent inspect original?:** **OPTIONAL**; useful for reasoning history.  
**Risk if omitted:** Moderate: it explains why the final model is single-class and 960px instead of simply P2/SAHI.

---

## 28 — `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`

**Sequence:** 28  
**Filename:** `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`  
**Approximate project phase:** Slice 3B final optimization evidence  
**Why introduced:** Document data-centric analysis and Candidate A selection.  
**Question/problem:** Why was baseline weak, which CV techniques should be applied, and did Candidate A solve the actual problem?  
**Important result:** It quantifies class imbalance, small objects, preprocessing chain, conservative augmentation, latency budget, single-class derived dataset, Candidate A configuration, Candidate A metrics (`mAP50 0.877`, `mAP50-95 0.609`, precision 0.901, recall 0.792), and SAHI rejection due to latency. fileciteturn24file1L65-L82 fileciteturn24file1L102-L125 fileciteturn24file1L129-L171 fileciteturn24file1L188-L216 fileciteturn24file1L220-L245  
**Effect on decisions:** This is the core Data-Centric AI reasoning record leading to Candidate A as deployment model.  
**Current relevance:** High historical/current model-selection context.  
**Status:** **EVIDENCE / CURRENT MODEL RATIONALE**  
**Related architecture/component:** YOLOv8n, single-class vehicle detector, dataset, SAHI.  
**Related decision:** Candidate A selected; P2 only if needed; SAHI deferred/rejected for runtime.  
**Related files:** `implementation_plan(1).md`, `SLICE_3C_PARETO_OPTIMIZATION.md`, `results.csv`, `labels.jpg`, `confusion_matrix_normalized.png`.  
**Equivalent/current content in Git repo?:** **PARTIAL / YES** in scripts/configs and current model choice, but exact analysis report is local evidence.  
**Must new agent inspect original?:** **YES** before changing the model/data strategy.  
**Risk if omitted:** High: agent may repeat rejected ideas or lose the data-centric rationale.

---

## 29 — `SLICE_3C_PARETO_OPTIMIZATION.md`

**Sequence:** 29  
**Filename:** `SLICE_3C_PARETO_OPTIMIZATION.md`  
**Approximate project phase:** Slice 3C Candidate A/B Pareto optimization and causal audit  
**Why introduced:** Compare Candidate A (`YOLOv8n`, 960, single-class) with Candidate B (`YOLOv8n-P2`) using a common accuracy/latency gate.  
**Question/problem:** Does P2 materially improve small-object recall enough to justify complexity/latency, and do we need distillation?  
**Important result:** Candidate A was confirmed as final. Candidate B got mAP50 0.878 vs A 0.877 and mAP50-95 0.615 vs 0.609, but recall decreased 0.783 vs 0.792. Fair latency audit later found Candidate A faster than B; P2 helped tiny objects only slightly and hurt larger-object recall. Distillation was explicitly marked unnecessary. fileciteturn24file2L255-L270 fileciteturn24file2L326-L365  
**Effect on decisions:** Locked Candidate A as the deployment model and closed data-centric optimization.  
**Current relevance:** **CURRENT MODEL SELECTION RECORD** immediately preceding TensorRT.  
**Status:** **CURRENT MILESTONE EVIDENCE**  
**Related architecture/component:** Candidate A/B, P2, latency benchmark.  
**Related decision:** A_WINS; Candidate B research artifact; distillation deferred.  
**Related files:** `implementation_plan(1).md`, `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`, `implementation_plan(3).md`, `implementation_plan(4).md`.  
**Equivalent/current content in Git repo?:** **PARTIAL / YES**; source/config and model choice are current, report itself may be local.  
**Must new agent inspect original?:** **YES** before proposing a different model architecture or retraining.  
**Risk if omitted:** High.

---

## 30 — `implementation_plan(3).md`

**Sequence:** 30  
**Filename:** `implementation_plan(3).md`  
**Approximate project phase:** Initial Slice 4 TensorRT FP32/FP16/INT8 plan, before toolchain remediation  
**Why introduced:** Define the deployment optimization path for Candidate A before the environment was ready.  
**Question/problem:** How should ONNX become TensorRT FP32/FP16/INT8, how should parity and latency be measured, and what are the engine-selection gates?  
**Important result:** The plan defines TensorRT toolchain audit, ONNX/shared detection contract, FP32 parity gate, FP16 parity gate, INT8 calibration using ~512 train images, engine-only/full-pipeline latency, final selection, and end-of-slice Git hygiene. It initially ended in `BLOCKED_TOOLCHAIN` because TensorRT/Python/trtexec/nvcc and CUDA runtime linking were missing. fileciteturn24file3L385-L396 fileciteturn24file3L404-L439  
**Effect on decisions:** Established Slice 4 as a deployment optimization slice, not another training slice.  
**Current relevance:** Current Slice 4 scope, but the original blocker section has been resolved through the later remediation plan and manual environment setup.  
**Status:** **CURRENT SCOPE / SUPERSEDED TOOLCHAIN STATE**  
**Related architecture/component:** TensorRT/ONNX, GPU runtime.  
**Related decision:** FP32 → FP16 → INT8 with measured parity and latency.  
**Related files:** `implementation_plan(4).md`, `SLICE_3C_PARETO_OPTIMIZATION.md`.  
**Equivalent/current content in Git repo?:** **PARTIAL / YES**, actual scripts/configs embody the scope.  
**Must new agent inspect original?:** **YES** for Slice 4 scope and acceptance gates; ignore its old missing-toolchain status if contradicted by current state.  
**Risk if omitted:** High: it contains the original acceptance thresholds and Git-closure requirements.

---

## 31 — `implementation_plan(4).md`

**Sequence:** 31  
**Filename:** `implementation_plan(4).md`  
**Approximate project phase:** Slice 4 toolchain remediation and manual-install handoff  
**Why introduced:** Resolve the initial TensorRT toolchain blocker without letting the agent silently mutate the host environment.  
**Question/problem:** What exactly is missing, what does each missing library/tool do, what CUDA family should be selected, and which environment changes should be performed by the user rather than the agent?  
**Important result:** It documents the role of TensorRT Python, `trtexec`, `nvcc`, `libcudart.so.13`, `LD_LIBRARY_PATH`, and ONNX Runtime CUDA EP; records CUDA 13 compatibility; recommends `tensorrt-cu13==11.2.1.2` and `onnxruntime-gpu==1.27.0`; and explicitly separates user-manual installation from agent-side source changes. fileciteturn24file4L462-L490  
**Effect on decisions:** User manually created `ml/.venv-tensorrt`, installed the chosen stack, and achieved `TENSORRT_IMPORT=PASS`, `TENSORRT_BUILDER=PASS`, `CUDA_RUNTIME=PASS`, `ORT_CUDA_EP=PASS`, `ONNX_INFERENCE=PASS`. This unlocked Slice 4.  
**Current relevance:** **CURRENT Slice 4 process/rationale**.  
**Status:** **CURRENT**  
**Related architecture/component:** TensorRT Python environment / CUDA 13.  
**Related decision:** Manual gate for critical environment changes; isolated TensorRT venv.  
**Related files:** `implementation_plan(3).md`, current TensorRT builder/parity scripts in repository, local `ml/.venv-tensorrt`.  
**Equivalent/current content in Git repo?:** **PARTIAL / YES** in source/process rules; exact plan is migration evidence.  
**Must new agent inspect original?:** **YES** before changing TensorRT environment or bypassing manual gates.  
**Risk if omitted:** High.

---

# C. SPECIAL RELATIONSHIPS AND EVOLUTION CHAINS

## C.1 `camera_gate_report.md` → `camera_gate_report(1).md`

This is one continuous camera bring-up sequence:

1. `camera_gate_report.md`: image transport worked but camera content was unusable for ArUco; marker detection FAIL. fileciteturn22file0L11-L13
2. `camera_gate_report(1).md`: camera optical axis was corrected and ground became visible; marker verification became the next separate gate. fileciteturn22file1L63-L79

The suffixed filename is **not** merely a duplicate or “newest master.” It is a later state report that supersedes the earlier camera-gate state.

## C.2 `walkthrough.md` → `walkthrough(1).md`

These refer to different phases:

- `walkthrough.md`: Slice 1 typed-interface/dashboard/C++ control engineering history and intermediate fixed-landing low-FPS calibration. fileciteturn22file2L96-L139
- `walkthrough(1).md`: later Slice 2 moving-platform walkthrough.

Therefore `walkthrough(1).md` should **not** be treated as “a newer version of the Slice 1 walkthrough.”

## C.3 `implementation_plan.md` → `implementation_plan(1).md` → `implementation_plan(3).md` → `implementation_plan(4).md`

Reconstructed sequence:

### `implementation_plan.md`
**Slice 3 baseline YOLO/ONNX.** Start with dataset validation → YOLOv8n baseline → evaluation → ONNX parity. Initially blocked by Roboflow access. fileciteturn23file3L256-L309

### `implementation_plan(1).md`
**Slice 3B optimization.** After baseline was judged too weak for the intended small-object/gimbal use case, introduce data-centric diagnosis, single-class vehicle, 960px input, conditional P2, and SAHI benchmark. fileciteturn24file0L9-L45

### `implementation_plan(3).md`
**Slice 4 TensorRT deployment optimization.** Candidate A is now frozen; build FP32 → FP16 → INT8, measure parity and latency, then close Git hygiene. It initially reports a missing toolchain. fileciteturn24file3L385-L439

### `implementation_plan(4).md`
**Slice 4 toolchain remediation.** Explain dependencies and establish a user-controlled isolated TensorRT environment rather than letting the agent silently install/change host state. fileciteturn24file4L462-L501

So the chain is **baseline ML → model optimization → deployment optimization → controlled environment remediation**.

## C.4 `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md` + `pid_moving.yaml` + `REPORT_C2(1).md`

These belong to the same Slice 2 moving-ArUco control evolution, but they represent different layers:

- `REPORT_C2(1).md` = detailed diagnosis of sign/frame/control behavior and failed/superseded runs. fileciteturn23file2L198-L246
- `pid_moving.yaml` = one concrete intermediate config snapshot that was later superseded. fileciteturn22file4L199-L215
- `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md` = later accepted Slice 2 outcome: moving platform at 0.10 m/s, tracking, touchdown ~0.067 m, fixed regression ~0.025 m. fileciteturn22file3L154-L189

A new agent must **not** copy the uploaded `pid_moving.yaml` blindly. The report explicitly records that `flip_x=false/flip_y=false` was a superseded unstable experiment. fileciteturn23file2L200-L222

## C.5 Slice 3 evidence chain

### `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`
Proves the baseline pipeline worked: dataset validation, 4-class YOLOv8n 640px training, baseline metrics, ONNX export, and initial parity. fileciteturn23file4L319-L340

### `confusion_matrix_normalized.png`
Visual evidence of class confusion that helped justify class collapse.

### `labels.jpg`
Visual evidence of label/object distribution and imbalance/small-object characteristics.

### `results.csv`
Per-epoch training history that supports learning-curve analysis and transfer-learning discussion.

### `val_batch0_labels.jpg`
Ground-truth visualization used for annotation sanity.

### `val_batch0_pred.jpg`
Prediction visualization used for baseline qualitative validation.

Together they support the transition from **baseline model measurement** to **data-centric model diagnosis**.

## C.6 Slice 3 vs Slice 3B vs Slice 3C

### Slice 3 — Baseline pipeline

Purpose: establish a reproducible weak baseline and ONNX reference.

Key result: YOLOv8n, 640px, 4 classes, baseline mAP50 0.501 / mAP50-95 0.335 in the acceptance snapshot. fileciteturn23file4L325-L340

Primary imported artifacts:
- `implementation_plan.md`
- `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`
- `results.csv`
- `labels.jpg`
- `confusion_matrix_normalized.png`
- `val_batch0_labels.jpg`
- `val_batch0_pred.jpg`

### Slice 3B — Data-centric / small-object optimization

Purpose: identify why the baseline was weak and apply targeted CV/data techniques.

Key decisions:
- collapse four vehicle classes to one `vehicle` class for the gimbal detector;
- increase input to 960px;
- conservative augmentation because many objects were tiny;
- benchmark SAHI rather than blindly adopting it.

Key result: Candidate A `YOLOv8n`, 960px, single-class, mAP50 0.877, mAP50-95 0.609, precision 0.901, recall 0.792. fileciteturn24file1L143-L170 fileciteturn24file1L188-L216

Primary imported artifacts:
- `implementation_plan(1).md`
- `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`

### Slice 3C — Pareto / causal / latency comparison

Purpose: verify whether P2 actually justifies its complexity and explain learning curves and latency.

Key result:
- Candidate A selected;
- Candidate B P2 only modestly increased mAP50-95 (+0.006) and decreased recall (-0.009);
- Candidate B's inference was slower;
- distillation was not necessary.

Primary imported artifact:
- `SLICE_3C_PARETO_OPTIMIZATION.md`

The later conversation also required a final causal/latency audit to verify initialization, fair latency, size-bin behavior, and operating-point fairness before Slice 4.

---

# D. CURRENT RELEVANCE MAP

## CURRENT / HIGH PRIORITY FOR NEW AGENT

1. `implementation_plan(4).md` — Slice 4 toolchain remediation and manual-gate rationale.
2. `SLICE_3C_PARETO_OPTIMIZATION.md` — final model selection rationale.
3. `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md` — data-centric model rationale.
4. `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md` — completed moving landing acceptance.
5. `REPORT_C2(1).md` — moving-control sign/frame history and rejected config rationale.
6. `SLICE_3_YOLO_ONNX_ACCEPTANCE.md` — baseline reference.
7. `ENGINEERING_UPGRADE_READINESS_REPORT.md` — original architecture/rationale snapshot, used historically.

## HISTORICAL / USE ONLY FOR SPECIFIC QUESTIONS

- `camera_gate_report.md`
- `camera_gate_report(1).md`
- `image(58).png`
- `debug_frame.png`
- `snapshot_report.md`
- `image(59).png`
- `image(60).png`
- `image(61).png`
- `image(62).png`
- `image(63).png`
- `Pasted text(3).txt`
- `REPORT_C1.md`
- `walkthrough.md`
- `walkthrough(1).md`
- `pid_moving.yaml`
- generated Slice-3 visual outputs (`labels.jpg`, `results.csv`, `val_batch0_labels.jpg`, `val_batch0_pred.jpg`, `confusion_matrix_normalized.png`)

## REPO-SUPERSEDED / NO LONGER CANONICAL

- `debug_frame.png` — previously a debug artifact and later removed from public tracking.
- `snapshot_report.md` — debug/snapshot artifact later removed from tracking.
- `pid_moving.yaml` — uploaded version is an intermediate config and is superseded by the accepted moving-control configuration.
- `camera_gate_report.md` — superseded as current status by later camera-gate success.
- `implementation_plan.md` — Slice 3 baseline plan is complete.
- `implementation_plan(1).md` — Slice 3B plan is complete.
- `implementation_plan(3).md` — its original `BLOCKED_TOOLCHAIN` state is superseded by `implementation_plan(4).md` and the successful manual installation.

---

# E. `MUST MIGRATE`

The following should be available to the new agent **before making any implementation decision**:

1. **`PROJECT_CONTINUITY.md`** — generated continuity checkpoint covering the overall project origin/evolution/current state. This is not one of the 31 user-imported files but was created specifically for migration.
2. **`SLICE_3C_PARETO_OPTIMIZATION.md`** — final Candidate A selection and Candidate B rejection rationale.
3. **`SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`** — data-centric diagnosis, small-object analysis, augmentation decisions, Candidate A reasoning.
4. **`SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`** — moving landing acceptance.
5. **`REPORT_C2(1).md`** — moving-control sign/frame diagnosis; critical to avoid repeating superseded `flip_x=false / flip_y=false` logic.
6. **`SLICE_3_YOLO_ONNX_ACCEPTANCE.md`** — baseline YOLO/ONNX reference.
7. **`implementation_plan(4).md`** — manual TensorRT toolchain decision and environment rationale.
8. **`implementation_plan(3).md`** — Slice 4 acceptance structure and FP32/FP16/INT8 gates.
9. **`ENGINEERING_UPGRADE_READINESS_REPORT.md`** — original architecture and Python/C++ responsibility rationale.

If disk/file migration is constrained, these nine are the highest-value context set.

---

# F. `KEEP AS HISTORICAL EVIDENCE`

Keep available, but do not treat as current source of truth:

- `REPORT_C1.md`
- `camera_gate_report.md`
- `camera_gate_report(1).md`
- `image(58).png`
- `debug_frame.png`
- `snapshot_report.md`
- `image(59).png`
- `image(60).png`
- `image(61).png`
- `image(62).png`
- `image(63).png`
- `Pasted text(3).txt`
- `walkthrough.md`
- `walkthrough(1).md`
- `confusion_matrix_normalized.png`
- `labels.jpg`
- `results.csv`
- `val_batch0_labels.jpg`
- `val_batch0_pred.jpg`

These artifacts matter for explaining how the project reached its current state, but they should not cause a new agent to revert to earlier states.

---

# G. `REPO ALREADY SUPERSEDES THIS FILE`

These files contain information now represented by current source/configuration and completed milestones, so the original artifact does not need to be loaded for ordinary execution:

- `implementation_plan.md`
- `implementation_plan(1).md`
- `camera_gate_report(1).md`
- `walkthrough.md`
- `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`
- `debug_frame.png`
- `snapshot_report.md`

For the last three, the repository and later evidence preserve the useful current outcome, while the historical artifact may still explain how it was reached.

---

# H. `UNKNOWN / NEED MANUAL INSPECTION`

The following points remain uncertain from the transcript/file evidence and should not be guessed:

1. Exact Git path / current tracked-path status of every imported report file. Some were explicitly local/ignored in conversation; the file attachment itself does not prove current Git tracking.
2. Exact content and role of the imported `implementation_plan` suffix files beyond the contents read here are known for `(1)`, `(3)`, and `(4)`; there may have been other non-uploaded plan text pasted directly in the conversation.
3. Exact original source of `0.609` and the later custom-test `0.52415` metric discrepancy was still being audited at the migration point. The latest conversation had not yet completed that provenance gate.
4. Exact Git commit/tag names for every historical Slice closure beyond those explicitly stated in conversation (`slice1-engineering-interface-v1`, `slice2-moving-aruco-v1`, `slice3c-vehicle-detector-v1`, and the later Slice 4 tag had not yet been created at the migration point).
5. Exact contents of any reports only mentioned in pasted logs but not imported (e.g. raw `REPORT_C2.md`, raw `REPORT_C3.md`, `FINAL_ACCEPTANCE_REPORT.md`).
6. Exact relationship between `ENGINEERING_UPGRADE_READINESS_REPORT.md` and any same-name/similar-name in-repo copy cannot be proven from attachment metadata alone.

---

# I. MIGRATION RISKS

## 1. Biggest risk: treating a historical artifact as current truth

The conversation repeatedly used reports as runtime evidence and then superseded them. A new agent must always prefer the latest accepted source/configuration and use old files to explain *why* it looks that way.

## 2. Slice 2 sign mapping can easily be broken

The uploaded `pid_moving.yaml` contains `flip_x=false/flip_y=false`, but later Slice 2 evidence explicitly marks that configuration as unstable and restores `flip_x=true`, `flip_y=true`. fileciteturn23file2L200-L222 A new agent must not copy the uploaded YAML blindly.

## 3. Fixed and moving landing share architecture but not identical tuning

The C++ PID is shared conceptually, but the moving mode had a separate conservative profile and moving-platform logic. The fixed Python baseline and moving C++ controller are not interchangeable tuning artifacts.

## 4. Model baseline vs deployment model can be confused

The 4-class YOLOv8n 640px baseline is a reference. Candidate A is the selected deployment model. The baseline's `mAP50=0.501` must not be treated as the current deployment performance; Candidate A was selected after data-centric optimization.

## 5. Slice 4 environment state evolved

`implementation_plan(3).md` says `BLOCKED_TOOLCHAIN`; `implementation_plan(4).md` documents the remediation and CUDA 13 environment decision. A new agent must use the later toolchain state, not reopen the original missing-dependency blocker without checking the current environment.

## 6. TensorRT engine is local and hardware-specific

Model binaries/dataset/engine/report artifacts were deliberately kept out of Git. The new agent needs scripts/configuration and local model availability, not a tracked binary dump.

## 7. Metric provenance at migration point is unresolved

The latest conversation was still auditing why a custom PyTorch test evaluator reported `mAP50-95=0.52415` while a previously cited `0.609` metric existed. Do not silently merge these into one metric. Determine whether `0.609` was a validation metric and the 0.52415 value is a test metric by running the official Ultralytics test reference.

## 8. Do not reintroduce rejected complexity prematurely

The conversation explicitly tested and rejected/superseded several approaches. Those decisions are listed below because a fresh agent may otherwise suggest them again.

---

# J. DO NOT REPEAT

## 1. Runtime camera `sed` pitch hacks

**Status:** REJECTED/SUPERSEDED.  
**Why:** exact ±90° camera edits caused singularity/NaN instability; modifying a running shell script also caused a bash syntax failure. Preserve camera model and diagnose via controlled configuration instead.

## 2. Aggressive C++ PID copied into low-FPS fixed path

**Status:** SUPERSEDED.  
**Why:** the original aggressive `kp/kd` combination was unstable under low camera FPS. The accepted fixed path mirrored the conservative Python P-only behavior rather than blindly restoring aggressive PID terms.

## 3. `flip_x=false`, `flip_y=false` for moving BODY-frame control

**Status:** REJECTED/SUPERSEDED.  
**Why:** Run 6 produced positive feedback/instability. Later evidence verified BODY-frame interpretation and restored `flip_x=true`, `flip_y=true`. fileciteturn23file2L200-L222

## 4. Static 30px pixel threshold as the only low-altitude moving landing gate

**Status:** SUPERSEDED.  
**Why:** pixel error grows as altitude falls and the camera FOV narrows, causing false aborts near the moving platform. Slice 2 moved toward scale-aware/final-commit/contact-confirmation behavior.

## 5. Blind `action.kill()` as normal moving-landing termination

**Status:** REJECTED/SUPERSEDED.  
**Why:** it is not a physically confirmed graceful touchdown contract. The final accepted moving termination uses physical contact confirmation plus disarm flow, not kill-as-normal-flow.

## 6. Kalman filter for Slice 2 moving landing

**Status:** REJECTED/DEFERRED for accepted Slice 2.  
**Why:** tracking became stable enough without it; adding Kalman would have increased scope before it was necessary. `REPORT_C2(1).md` explicitly records `Kalman proposal: REJECTED_NOT_NEEDED`. fileciteturn23file2L214-L222

## 7. SAHI as runtime detector path

**Status:** REJECTED/DEFERRED.  
**Why:** it increased latency to ~90.98 ms in the recorded benchmark, exceeding the 30 FPS budget. fileciteturn24file1L220-L234

## 8. YOLOv8n-P2 as final model

**Status:** REJECTED as deployment model / kept as research artifact.  
**Why:** only +0.006 mAP50-95, lower recall, and slower inference; did not pass the predefined Pareto gate. fileciteturn24file2L326-L365

## 9. Distillation immediately after Candidate A/B comparison

**Status:** DEFERRED/NOT NEEDED.  
**Why:** Candidate A already met the current accuracy/latency goal on RTX 3060, and there was no teacher sufficiently better to justify the extra training complexity. fileciteturn24file2L363-L365

## 10. Full TensorRT system install just to get `nvcc`

**Status:** NOT REQUIRED for current Python TensorRT slice.  
**Why:** `nvcc` is needed later for CUDA/C++ compilation/custom plugin work, but the current Python TensorRT build path did not require it. `implementation_plan(4).md` explicitly documents this. fileciteturn24file4L464-L471

## 11. Letting the agent silently install/upgrade the user's environment

**Status:** REJECTED working style.  
**Why:** user explicitly wanted high-impact environment/training decisions to be visible and manually controlled. The TensorRT toolchain remediation therefore used an isolated venv and a user-run installation gate. fileciteturn24file4L500-L501

## 12. Training again just to optimize latency before TensorRT

**Status:** DEFERRED.  
**Why:** changing weights without changing the graph does not materially change dense inference cost; the next correct lever is TensorRT FP32/FP16/INT8 benchmarking.

---

# K. WORKING STYLE FOR THE NEW AGENT

These preferences were explicit and repeated in the conversation:

1. **Stay narrow and mission-driven.** The user repeatedly asked for progress to be accelerated and for the agent not to wander into unrelated refactors or optional work.
2. **Always preserve the working baseline.** Do not break `make demo-python` or `make demo-cpp` unless a slice explicitly requires a controlled change.
3. **Explain new concepts when they materially affect a decision.** Examples: TensorRT vs `trtexec` vs `nvcc`, CUDA runtime libraries, calibration, P2/SAHI tradeoffs.
4. **Hand major manual decisions to the user.** The user wants to run important training and environment setup in the foreground and observe epoch/metrics/progress themselves.
5. **Agent can prepare exact commands, scripts, tests, and decision matrices.** User performs the high-impact manual run when the decision affects the development environment or model training.
6. **Use gate-based development.** Each important change should have a focused acceptance gate; do not stack many experimental changes without knowing which caused the result.
7. **At the end of every Slice, show a short overall progress checkpoint.** The user explicitly requested this to prevent scope drift.
8. **End each Slice with Git hygiene.** Before the next Slice: acceptance PASS → forbidden-artifact scan → selective stage → commit → merge to main → annotated tag → clean working tree.
9. **Never push remote unless the user explicitly asks.**
10. **Do not track datasets/model binaries/generated ML reports.** They must remain local/ignored unless the user explicitly changes that policy.
11. **Be explicit about uncertainty.** If a file/metric/path cannot be reconstructed, say `UNKNOWN` rather than guess.
12. **Prefer practical engineering explanations over abstract theory.** The user wants to understand why an engineering decision is being made, but not a broad lecture unrelated to the current blocker.

---

# L. FINAL MIGRATION CHECKLIST

Before the new agent begins implementation:

- [ ] Load `PROJECT_CONTINUITY.md`.
- [ ] Load `MIGRATION_FILE_MANIFEST.md`.
- [ ] Inspect current Git repository state.
- [ ] Verify the current branch/main/tag state rather than trusting historical snapshots.
- [ ] Verify Slice 3C is merged and Candidate A is still the selected model.
- [ ] Verify current local TensorRT venv state before reopening toolchain installation.
- [ ] Verify current Slice 4 work-in-progress and exact last command/result.
- [ ] Do not treat local generated reports as current source of truth when source/config has moved on.

---

# MUST MIGRATE

**Highest-value minimal set for the new workspace:**

1. `PROJECT_CONTINUITY.md`
2. `MIGRATION_FILE_MANIFEST.md`
3. `SLICE_3C_PARETO_OPTIMIZATION.md`
4. `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`
5. `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`
6. `SLICE_2_MOVING_ARUCO_ACCEPTANCE.md`
7. `REPORT_C2(1).md`
8. `implementation_plan(3).md`
9. `implementation_plan(4).md`
10. `ENGINEERING_UPGRADE_READINESS_REPORT.md`

If only five artifacts can be migrated, prioritize:

1. `PROJECT_CONTINUITY.md`
2. `MIGRATION_FILE_MANIFEST.md`
3. `SLICE_3C_PARETO_OPTIMIZATION.md`
4. `SLICE_3B_SMALL_OBJECT_OPTIMIZATION.md`
5. `implementation_plan(4).md`

---

# KEEP AS HISTORICAL EVIDENCE

- `REPORT_C1.md`
- `camera_gate_report.md`
- `camera_gate_report(1).md`
- `image(58).png`
- `debug_frame.png`
- `snapshot_report.md`
- `image(59).png`
- `image(60).png`
- `image(61).png`
- `image(62).png`
- `image(63).png`
- `Pasted text(3).txt`
- `walkthrough.md`
- `walkthrough(1).md`
- `confusion_matrix_normalized.png`
- `labels.jpg`
- `results.csv`
- `val_batch0_labels.jpg`
- `val_batch0_pred.jpg`

These are historical/evidence artifacts, not current implementation truth.

---

# REPO ALREADY SUPERSEDES THIS FILE

- `camera_gate_report.md`
- `camera_gate_report(1).md`
- `debug_frame.png`
- `snapshot_report.md`
- `implementation_plan.md`
- `implementation_plan(1).md`
- `walkthrough.md`
- `SLICE_3_YOLO_ONNX_ACCEPTANCE.md`

The current repository/source and later Slice evidence preserve the useful final implementation state. The historical files remain useful for rationale but are not required for ordinary execution.

---

# UNKNOWN / NEED MANUAL INSPECTION

- Exact current Git tracking status of every imported report/image; attachment metadata alone does not prove tracking.
- Exact contents of the raw log/report files mentioned only in pasted terminal output (`REPORT_C2.md`, `REPORT_C3.md`, `FINAL_ACCEPTANCE_REPORT.md`, `FINAL_TERMINATION_REPORT.md`) because they were not all imported as attachments in this conversation.
- Exact provenance of the `0.609` model metric versus the later `0.52415` custom test metric was still under investigation at the migration point.
- Exact in-repo paths for some historically uploaded artifacts.
- Any claim about a file that cannot be backed by the attachment content or explicit surrounding conversation should be treated as `UNKNOWN`.

---

# END OF MIGRATION FILE MANIFEST
